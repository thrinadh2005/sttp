# Secure Penetration Testing Toolkit (SPTT) - Dependencies

# Core dependencies
colorama>=0.4.6          # For colored CLI output
tabulate>=0.9.0          # For table formatting
validators>=0.20.0       # For input validation

# Web interface
flask>=2.3.0             # Web framework
flask_socketio>=5.3.0    # WebSocket support for real-time logs

# Optional dependencies for enhanced features
# cryptography>=41.0.0   # For hash generation (if needed)
# requests>=2.31.0      # For HTTP-based testing (if needed)
